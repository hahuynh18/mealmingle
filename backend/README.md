Provides a Express.js backend to process images using the Google Cloud Vision API. It detects text and labels from uploaded images.

Upload or send base64 images
Extract text (textDetection)
Extract labels (labelDetection)
Returns cleaned JSON response and adds items to user's inventory on the Railway database
