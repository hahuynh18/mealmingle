# Database Folder

This folder contains all database-related files for the project.

## Files
- `init.sql` – Creates the initial database tables.
- `seed.sql` – Adds sample data.
- `config.json` – Stores database connection details.

## Notes
The database is designed for PostgreSQL (or MySQL, etc.).
Run these scripts in the order listed above when setting up the project locally.
